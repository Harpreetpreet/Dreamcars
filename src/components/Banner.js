import React from 'react';
import styled from 'styled-components';

const Container = styled.div``;

const Banner = () => (
  <Container>
    <div>hello banner</div>
  </Container>
);
export default Banner;
